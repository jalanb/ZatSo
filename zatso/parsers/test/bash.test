zatso.parses.bash
=================

    >>> from zatso.parsers import bash
    >>> assert bash.__doc__

    >>> from pysyte.types import paths

Parse ~/.bashrc
---------------

    >>> p = paths.bashrc
    >>> assert p.name == '.bashrc'
    >>> assert p.isfile()

Real test is here:
    can it parse bash sources?

    >>> ast = parsers.bash.parse(p)

This is a guess, and likely only works for me

    >>> assert "bashrc" in ast.functions
